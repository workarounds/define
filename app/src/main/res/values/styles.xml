<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->

        <item name="android:colorPrimary">@color/theme_primary</item>
        <item name="android:colorPrimaryDark">@color/theme_primary_dark</item>
        <item name="defaultFontName">OpenSans</item>

    </style>

    <style name="MyToolBar" parent="AppTheme">
        <item name="android:color">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
    </style>

    <style name="BaseHeader" parent="TextAppearance.AppCompat">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/dark_gray</item>
        <item name="android:textSize">@dimen/font_medium</item>
        <item name="android:layout_margin">@dimen/margin_regular</item>
    </style>

    <style name="BubbleAnimations">
        <item name="android:windowEnterAnimation">@anim/slide_in_from_left</item>
        <item name="android:windowExitAnimation">@anim/slide_out_to_left</item>
    </style>

    <style name="CardAnimations">
        <item name="android:windowEnterAnimation">@anim/card_entry</item>
        <item name="android:windowExitAnimation">@anim/card_exit</item>
    </style>
</resources>

<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->

        <item name="colorPrimary">@color/theme_primary</item>
        <item name="colorPrimaryDark">@color/theme_primary_dark</item>
        <item name="defaultFontName">OpenSans</item>
        <item name="colorAccent">@color/theme_primary_dark</item>

    </style>

    <!-- Translucent Activity -->
    <style name="Theme.CustomTranslucent" parent="AppTheme">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowEnterAnimation">@null</item>
        <item name="android:windowExitAnimation">@null</item>
        <item name="android:windowShowAnimation">@null</item>
        <item name="android:windowHideAnimation">@null</item>
    </style>

    <style name="MyToolBar" parent="AppTheme">
        <item name="android:color">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
    </style>

    <style name="BaseHeader" parent="TextAppearance.AppCompat">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/dark_gray</item>
        <item name="android:textSize">@dimen/font_medium</item>
        <item name="android:layout_margin">@dimen/margin_regular</item>
    </style>

    <style name="BubbleAnimations">
        <item name="android:windowEnterAnimation">@anim/slide_in_from_left</item>
        <item name="android:windowExitAnimation">@anim/slide_out_to_left</item>
    </style>

    <style name="CardAnimations">
        <item name="android:windowEnterAnimation">@anim/card_entry</item>
        <item name="android:windowExitAnimation">@anim/card_exit</item>
    </style>

    <style name="ActionButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textColor">@color/theme_accent</item>
        <item name="android:minHeight">@dimen/bar_medium</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:paddingLeft">@dimen/padding_large</item>
        <item name="android:paddingRight">@dimen/padding_large</item>
        <item name="android:theme">@style/ActionButtonTheme</item>
    </style>

    <style name="ActionButtonTheme" parent="Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/cardview_light_background</item>
        <item name="android:colorButtonNormal">@color/cardview_light_background</item>
    </style>
</resources>
